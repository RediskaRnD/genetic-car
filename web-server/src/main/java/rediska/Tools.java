package rediska;

public class Tools {

//    // угол растёт по часовой стрелке
//    static public Point lineToAngle(Point p, float distance, float angle) {
//        return new Point(p.x + (int) (distance * Math.cos(angle)), p.y + (int) (distance * Math.sin(angle)));
//    }
}
